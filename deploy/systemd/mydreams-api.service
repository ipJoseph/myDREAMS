[Unit]
Description=DREAMS Property API
After=network.target

[Service]
Type=simple
User=dreams
Group=dreams
WorkingDirectory=/opt/mydreams/apps/property-api
Environment="PATH=/opt/mydreams/venv/bin:/usr/local/bin:/usr/bin:/bin"
Environment="PYTHONUNBUFFERED=1"
ExecStart=/opt/mydreams/venv/bin/python app.py
Restart=always
RestartSec=5

# Logging
StandardOutput=journal
StandardError=journal
SyslogIdentifier=mydreams-api

# Security hardening
NoNewPrivileges=true
PrivateTmp=true
ProtectSystem=strict
ReadWritePaths=/opt/mydreams/data /opt/mydreams/logs

[Install]
WantedBy=multi-user.target
